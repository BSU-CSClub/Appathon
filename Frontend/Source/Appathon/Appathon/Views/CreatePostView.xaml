<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Appathon.CreatePostView">
	<ContentView.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
		<Label Grid.Row="0" Text="{Binding Username}" />
		<Entry Grid.Row="1" Text="{Binding Title}"/>
		<Editor Grid.Row="2" Text="{Binding Contents}"/>
		<Button Text="Upload" Grid.Row="3" Command="{Binding UploadPostCommand}"/>
		</Grid>
	</ContentView.Content>
</ContentView>
